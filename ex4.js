// 4. Multiplique a idade de todos usuários por dois e depois realize um filtro nos usuários 
//que possuem no máximo 50 anos

const data = require("./data.json");

function op (data){
    return age*2;
}
function idade(age){
    if(age<50){
        return first_name.age;
    }
}
console.log('idade total'+op);